// This is the page connected to the endpoint "/read"
const ReadPage = () => {
    return(
        <main>
            { /* placeholder for testing */ }
        </main>
    );
};

export default ReadPage;